import React from "react";
import Navbar from "../layouts/Navbar";
import { WalletsProps } from "../utils/interface";

const Wallets: React.FC<WalletsProps> = ({}) => {
  return (
    <>
      <Navbar />
    </>
  );
};
export default Wallets;
